<%= form_for(@game_genre) do |f| %>
  <% if @game_genre.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@game_genre.errors.count, "error") %> prohibited this game_genre from being saved:</h2>

      <ul>
      <% @game_genre.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name_jp %><br>
    <%= f.text_field :name_jp %>
  </div>
  <div class="field">
    <%= f.label :name_en %><br>
    <%= f.text_field :name_en %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
